version: 2.1

jobs:
  test:
    docker:
      # the Docker image with Cypress dependencies
      - image: cypress/base:8
        environment:
          ## this enables colors in the output
          TERM: xterm
    working_directory: ~/app
    steps:
      - checkout
      - restore_cache:
          keys:
            - v2-deps-{{ .Branch }}-{{ checksum "e2e/package-lock.json" }}
            - v2-deps-{{ .Branch }}-
            - v2-deps-
      - run: cd e2e && npm ci
      - save_cache:
          key: v2-deps-{{ .Branch }}-{{ checksum "e2e/package-lock.json" }}
          paths:
            - ~/.npm
            - ~/.cache
      - run:
          name: Install jq
          command: cd e2e && apt-get install jq         
      - run:
          name: Running E2E tests with recording
          when: on_success
          command: cd e2e && make test-record  
      - run:
          name: Combine MochaAwesome Reports & publish to AWS
          when: always
          command: cd e2e && make reports-combine-publish-s3       
      - store_test_results:
          path: e2e/cypress/reports
      - store_artifacts:
          path: e2e/reports/mochareports
      - store_artifacts:
          path: e2e/cypress/videos
      - store_artifacts:
          path: e2e/cypress/screenshots
      - run:
          name: Slack Custom Reporting
          when: always
          command: ./.circleci/slack-alert.sh

workflows:
  build-test:
    jobs:
      - test